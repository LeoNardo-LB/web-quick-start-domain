# 测试代码编写规范

> **核心原则**：
> - 单元测试不启动Spring上下文，集成测试启动最小化Spring上下文
> - 质量要求：100%通过率，95%行覆盖率，100%分支覆盖率
> - **所有测试代码必须在test模块中**（test/src/test/java）
>
> **本规范是AI生成测试代码的唯一标准，任何违反本规范的代码生成请求必须被拒绝**

---

## 🚨 重要约束（强制执行）

### 1. 测试代码位置约束

**❌ 禁止事项**：

- ❌ **禁止**在业务模块（domain、app、adapter、infrastructure）的`src/test/java`中放置测试代码
- ❌ **禁止**在业务模块的`pom.xml`中添加测试依赖（junit、mockito等）
- ❌ **禁止**业务模块依赖test模块

**✅ 正确做法**：

- ✅ **所有测试代码必须放在test模块中**（`test/src/test/java`）
- ✅ 测试模块（test）依赖所有业务模块
- ✅ 通过Maven依赖传递，test模块可以访问所有业务代码

**原因**：

- 避免循环依赖（业务模块 → test → start → 业务模块）
- 统一测试管理
- 清晰的模块职责分离

### 2. 测试模块依赖关系

```
test → start → adapter → app → infrastructure → domain
```

**说明**：

- test模块依赖start模块
- start模块依赖所有业务模块
- 通过传递依赖，test模块可以访问所有业务代码进行测试

---

## 一、项目测试结构

### 1.1 项目目录结构

```
web-quick-start-domain/
├── adapter/                          # 接口层模块
│   └── src/main/java/               # 只有源代码，无测试代码
├── app/                              # 应用层模块
│   └── src/main/java/               # 只有源代码，无测试代码
├── domain/                           # 领域层模块
│   └── src/main/java/               # 只有源代码，无测试代码
├── infrastructure/                   # 基础设施层模块
│   └── src/main/java/               # 只有源代码，无测试代码
├── start/                            # 启动模块
│   └── src/main/java/
└── test/                             # ⭐ 独立测试模块（所有测试代码都在这里）
    └── src/
        └── test/java
            ├─cases
            │  ├─integrationtest    # 集成用例存放处
            │  └─unittest           # 单测用例存放处
            ├─fixture
            └─support
```

**⚠️ 关键规则**：

1. **所有测试代码必须在test模块中**
2. **业务模块（domain/app/adapter/infrastructure）不应包含src/test目录**
3. **业务模块的pom.xml不应包含测试依赖**

### 1.2 DDL文件约束

```
start/src/main/resources/config/schema.sql  # 唯一DDL文件，只读
```

**DDL修改约束**：

- ❌ **禁止**在测试代码中修改 `schema.sql` 文件
- ❌ **禁止**在运行时动态创建/修改表结构
- ❌ **禁止**执行任何DDL语句（CREATE/ALTER/DROP TABLE）
- ✅ **允许**在测试配置中引用此文件
- 🔒 **所有DDL变更必须通过版本控制流程**
- ⚠️ **测试只能读取DDL，不能写入或修改**

---

## 二、包名映射规则

### 2.1 测试类命名规范

| 测试类型     | 命名格式       | 示例                                           |
|----------|------------|----------------------------------------------|
| **单元测试** | `XxxUTest` | `UserServiceUTest`, `StringUtilsUTest`       |
| **集成测试** | `XxxITest` | `UserControllerITest`, `UserRepositoryITest` |

> `UTest`代表`UnitTest`单元测试，`ITest`代表`ITest`集成测试

### 2.2 包名、测试类对应关系

单测/集测包名是他们自己的包名加上业务包名，例如：

- 业务包名：org.smm.archetype.xxx
- 单测包名：cases.unittest.org.smm.archetype.xxx
- 集测包名：cases.integrationtest.org.smm.archetype.xxx

类名命名规则：

- 业务类名：XxxService
- 单测类名：XxxServiceUTest
- 集成测试类名：XxxServiceITest

---

## 三、单元测试规范（**不启动Spring上下文**）

### 3.1 ✅ 允许的操作

```java
package org.smm.archetype.domain.service;

import support.UnitTestBase;
import org.junit.jupiter.api.Test;
import org.mockito.InjectMocks;
import org.mockito.Mock;

class OrderServiceUTest extends UnitTestBase {

    @Mock
    private OrderRepository orderRepository;

    @Mock
    private PaymentService paymentService;

    @InjectMocks
    private OrderService orderService;

    @Test
    void createOrder_ValidOrder_ReturnsOrder() {
        // Arrange
        Order order = new Order("test@example.com");
        when(orderRepository.save(any(Order.class))).thenReturn(order);

        // Act
        Order result = orderService.createOrder(order);

        // Assert
        assertNotNull(result.getId());
        assertEquals("test@example.com", result.getEmail());
    }

    @Test
    void createOrder_DuplicateEmail_ThrowsException() {
        // Arrange
        Order order = new Order("existing@example.com");
        when(orderRepository.existsByEmail("existing@example.com")).thenReturn(true);

        // Act & Assert
        assertThrows(DuplicateOrderException.class, () -> orderService.createOrder(order));
    }
}
```

### 3.2 ❌ 绝对禁止的操作

```java
// 禁止Spring注解
@SpringBootTest  // ❌ 绝对禁止
@Autowired       // ❌ 绝对禁止

// 禁止数据库操作
DataSource dataSource = ...;  // ❌ 绝对禁止
Connection conn = ...;        // ❌ 绝对禁止

// 禁止DDL修改
String alterTable = "ALTER TABLE order ADD COLUMN new_column VARCHAR(255)"; // ❌ 绝对禁止

// 禁止文件操作
Files.

write(Paths.get("../schema.sql"), "MODIFIED CONTENT".

getBytes()); // ❌ 绝对禁止
```

### 3.3 单元测试最佳实践

1. **使用Mock隔离依赖**
    - Repository使用Mock
    - 外部服务使用Mock
    - 不访问真实数据库

2. **覆盖所有分支**
    - 正常流程
    - 异常流程
    - 边界条件

3. **快速执行**
    - 不启动Spring上下文 - 执行时间应 < 100ms

---

## 四、集成测试规范（**启动Spring上下文**）

### 4.1 ✅ 允许的操作

```java
package org.smm.archetype.adapter.controller;

import support.ITestBase;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.mock.mockito.MockBean;

class OrderControllerITest extends ITestBase {

    @Autowired
    private MockMvc mockMvc;

    @Override
    protected String getDataSetFile() {
        return "controller/order-data.xml"; // 使用预定义数据集
    }

    @Test
    void getOrderById_ExistingOrder_Returns200() throws Exception {
        mockMvc.perform(get("/api/orders/1"))
               .andExpect(status().isOk())
               .andExpect(jsonPath("$.id").value(1))
               .andExpect(jsonPath("$.email").value("test@example.com"));
    }

    // 允许Mock外部服务
    @MockBean
    private RpcPaymentService rpcPaymentService;
}
```

### 4.2 ❌ 禁止的操作

```java
// 禁止Mock数据库
@MockBean
private OrderRepository orderRepository; // ❌ 集成测试禁止Mock数据库

// 禁止DDL操作
@Test
void testSchemaModification() {
    jdbcTemplate.execute("ALTER TABLE order ADD COLUMN temp_column INT"); // ❌ 绝对禁止
}

// 禁止动态表创建
@Test
void testDynamicTableCreation() {
    jdbcTemplate.execute("CREATE TABLE temp_test (id INT)"); // ❌ 绝对禁止
}

// 禁止修改DDL文件
Files.

write(Paths.get("../schema.sql"), "MODIFIED CONTENT".

getBytes()); // ❌ 绝对禁止
```

### 4.3 集成测试最佳实践

#### ⚠️ 核心原则：必须使用DBUnit管理测试数据

**强制要求**：

- ✅ **所有测试数据必须通过DBUnit加载**
- ❌ 禁止在测试代码中直接调用ApplicationService/Repository创建数据
- ❌ 禁止使用HTTP POST请求在@BeforeEach中创建测试数据
- ❌ 禁止在测试方法中编写数据准备逻辑

**原因**：

1. **数据一致性**：DBUnit使用XML文件定义测试数据，数据结构清晰可见
2. **测试隔离**：CLEAN_INSERT确保每个测试方法的数据独立
3. **可维护性**：测试数据与测试代码分离，易于维护
4. **版本控制**：XML数据集文件可以纳入版本控制，数据变更可追溯

**正确示例**：

```java
class OrderControllerITest extends ITestBase {

    @Override
    protected String getDataSetFile() {
        return "controller/order-data.xml"; // ✅ 使用DBUnit数据集
    }

    @Test
    void getOrderById_ExistingOrder_Returns200() throws Exception {
        // 测试逻辑，数据已在@BeforeEach中通过DBUnit加载
        mockMvc.perform(get("/api/orders/1"))
                .andExpect(status().isOk());
    }

}
```

**错误示例**：

```java

@BeforeEach
void setUpTestData() {
    // ❌ 禁止：直接调用Service创建数据
    orderApplicationService.createOrder(request);

    // ❌ 禁止：使用HTTP POST创建数据
    mockMvc.perform(post("/api/orders").content(json));

    // ❌ 禁止：直接操作数据库
    jdbcTemplate.execute("INSERT INTO order VALUES (...)");
}
```

#### 其他最佳实践

1. **Mock外部服务**
    - 使用@MockBean Mock外部RPC服务
    - 不Mock数据库

2. **测试完整流程**
    - Controller → Application → Domain → Repository
    - 验证HTTP请求/响应

3. **数据集文件组织**
    - 按功能模块组织：`datasets/integration/controller/`、`datasets/integration/service/`
    - 每个测试类对应独立的数据集文件
    - 数据集文件命名：{类名}-data.xml

---

## 五、测试基类使用

### 5.1 UnitTestBase（纯单元测试基类）

**位置**：`test/src/test/java/support/UnitTestBase.java`

**特点**：

- ✅ 0毫秒启动时间
- ✅ 完全隔离，无外部依赖
- ✅ 可Mock任意对象
- ❌ 不能测试Spring集成

**使用示例**：

```java
class OrderServiceUTest extends UnitTestBase {

    @Mock
    private OrderRepository orderRepository;

    @InjectMocks
    private OrderService orderService;

    @Test
    void testSomeMethod() {
        // 测试逻辑
    }

}
```

### 5.2 ITestBase（集成测试基类）

**位置**：`test/src/test/java/support/ITestBase.java`

**特点**：

- ✅ 启动完整的Spring上下文
- ✅ 使用H2内存数据库
- ✅ 支持DBUnit数据预加载
- ✅ 事务回滚保证隔离性
- ⚠️ 启动时间 ~200-500ms

**可重写的方法**：

```java
private String getDataSetFile() {
    return null; // 子类重写，指定数据集文件
}

private boolean shouldLoadTestData() {
    return getDataSetFile() != null; // 是否加载测试数据
}

private void cleanDatabase() throws Exception {
    // 清理整个数据库 - 子类可调用
}

private void loadDataSet(String dataSetFile) throws Exception {
    // 加载DBUnit数据集 - 已实现
}

protected void verifyDatabaseState(String expectedDataSetFile) throws Exception {
    
```

**使用示例**：

```java
class OrderControllerITest extends ITestBase {

    @Autowired
    private MockMvc mockMvc;

    @Override
    protected String getDataSetFile() {
        return "controller/order-data.xml";
    }

    @Test
    void getOrderById_ExistingOrder_Returns200() throws Exception {
        mockMvc.perform(get("/api/orders/1"))
                .andExpect(status().isOk());
    }

    @Test
    void createOrder_ValidRequest_Returns201() throws Exception {
        // 测试创建逻辑
        verifyDatabaseState("controller/order-data-after-create.xml");
    }

}
```

---

## 六、DBUnit数据管理

### 6.1 数据集文件位置

```
test/src/test/resources/datasets/integration/
├── controller/
│   ├── order-data.xml
│   └── user-data.xml
└── service/
    ├── payment-data.xml
    └── shipping-data.xml
```

### 6.2 数据集文件示例

**`test/src/test/resources/datasets/integration/controller/order-data.xml`**：

```xml
<?xml version="1.0" encoding="UTF-8"?>
<dataset>
    <!-- 订单表数据 -->
    <order
            id="1"
            email="john.doe@example.com"
            status="CREATED"
            total_amount="99.99"
            created_at="2024-01-01 10:00:00"
    />

    <order
            id="2"
            email="jane.smith@example.com"
            status="PAID"
            total_amount="199.99"
            created_at="2024-01-01 11:00:00"
    />

    <!-- 订单项表数据 -->
    <order_item
            id="1"
            order_id="1"
            product_name="Laptop"
            quantity="1"
            price="99.99"
    />
</dataset>
```

### 6.3 数据加载流程

```
1. @BeforeEach 执行
   ↓
2. 调用 getDataSetFile() 获取数据集文件
   ↓
3. 调用 loadDataSet() 加载数据
   ↓
4. 执行测试方法
   ↓
5. @Transactional 自动回滚
```

### 6.4 数据验证

```java

@Test
void createOrder_ValidRequest_Returns201() throws Exception {
    // Arrange
    String requestJson = "{\"email\":\"new@example.com\"}";

    // Act
    mockMvc.perform(post("/api/orders")
                            .contentType(MediaType.APPLICATION_JSON)
                            .content(requestJson))
            .andExpect(status().isCreated());

    // Assert - 验证数据库状态
    verifyDatabaseState("controller/order-data-after-create.xml");
}
```

---

## 七、集成测试配置

### 7.1 配置文件位置

**`test/src/test/resources/config/application-integration.yaml`**

### 7.2 配置示例

```yaml
spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=MySQL;DATABASE_TO_LOWER=TRUE;FILE_LOCK=NO
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
  sql:
    init:
      mode: always
      # 引用DDL文件，只读模式
      schema-locations: classpath:config/schema.sql

# DBUnit Configuration
dbunit:
  datatype-factory: com.github.springtestdbunit.datatype.MySqlDataTypeFactory
  escape-pattern: "`?`"
  case-sensitive-table-names: false
  isolation: METHOD
```

**重要说明**：

- ✅ 使用 `classpath:config/schema.sql` 加载DDL
- ❌ 不使用 `file:../../../../schema.sql`
- ✅ DDL文件位于 `start/src/main/resources/config/schema.sql`

---

## 八、覆盖率要求（**绝对强制**）

### 8.1 覆盖率红线

| 指标          | 要求   | 处理方式          |
|-------------|------|---------------|
| **通过率**     | 100% | 任何失败立即停止生成    |
| **行覆盖率**    | ≥95% | 不足时补充边界条件测试   |
| **分支覆盖率**   | 100% | 不足时补充所有分支路径   |
| **DDL修改次数** | 0    | 任何DDL修改尝试立即拒绝 |

### 8.2 覆盖率检查命令

```bash
# 运行所有测试
mvn test

# 查看覆盖率报告
mvn jacoco:report
```

---

## 九、关键约束总结

### 9.1 目录与包名约束

- **测试模块**：所有测试代码必须在各模块的 `src/test/java/` 中
- **包名前缀**：无强制cases前缀，使用实际业务包名
- **类名后缀**：单元测试用 `XxxUTest`，集成测试用 `XxxITest`
- **基类位置**：`support.UnitTestBase.java` 和 `support.IntegrationTestBase.java`

### 9.2 DDL安全约束

- **文件位置**：`start/src/main/resources/config/schema.sql`
- **访问模式**：只读，禁止任何写操作
- **SQL约束**：禁止在测试中执行任何DDL语句
- **路径引用**：配置文件中使用 `classpath:config/schema.sql`

### 9.3 DBUnit数据管理约束

- **数据加载时机**：`@BeforeEach` + `@Transactional`
- **操作类型**：使用 `CLEAN_INSERT` 保证数据隔离
- **状态验证**：使用 `verifyDatabaseState()` 验证数据状态
- **文件命名**：数据集文件按功能模块组织，避免共享数据集

### 9.4 环境隔离约束

- **数据库隔离**：每个测试方法使用独立的H2内存数据库实例
- **文件隔离**：测试不能修改任何项目文件，特别是schema.sql文件
- **状态隔离**：每个测试方法执行前后，环境状态必须完全一致
- **Mock隔离**：单元测试Mock所有依赖，集成测试只Mock外部服务

---

## 十、测试结果报告格式

每次生成测试代码后，必须提供以下格式的测试结果报告：

```markdown
# 测试生成结果报告

**生成时间**：2026-01-11 10:30:00
**测试位置**：test/src/test/java/
**DDL文件**：start/src/main/resources/config/schema.sql (只读)
**规范版本**：v2.0.0

## 质量指标

| 指标 | 要求 | 实际 | 状态 |
|------|------|------|------|
| 通过率 | 100% | 100% | ✅ |
| 行覆盖率 | ≥95% | 96.2% | ✅ |
| 分支覆盖率 | 100% | 100% | ✅ |
| DDL修改次数 | 0 | 0 | ✅ |

## 生成文件统计

- **单元测试文件**：8个 (`XxxTest`)
- **集成测试文件**：4个 (`XxxITest`)
- **数据集文件**：12个

## DDL安全检查

✅ **DDL文件访问**：只读模式，无写入操作
✅ **动态DDL**：未检测到CREATE/ALTER/DROP语句
✅ **文件操作**：无对schema.sql的修改尝试

## 验证结论

✅ **所有测试文件符合规范要求**
✅ **DDL文件安全**
✅ **环境隔离完整**
✅ **可以安全提交到代码库**
```

---

## 十一、相关文档

- **业务代码编写规范**：[业务代码编写规范.md](业务代码编写规范.md) - 第9章测试规范概述
- **代码生成工作流**：[代码AI生成工作流.md](AI生成代码工作流.md) - 4步验证流程
